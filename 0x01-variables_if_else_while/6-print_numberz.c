#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: Print all numbers using putchar
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	int digit = 0;

	while (digit <= 9)
	{
		putchar(digital + '0');
		digit++;
	}
	putchar('\n');

	return (0);
}
